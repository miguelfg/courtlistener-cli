# Parties Examples

List parties:

```bash
courtlistener-cli parties list --limit 20
courtlistener-cli parties list --docket 4134326 --limit 100 --format json
courtlistener-cli parties list --docket 4134326 --filter-nested-results --format xlsx --output ./output
```
