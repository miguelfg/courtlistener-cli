# Tags Examples

List tags:

```bash
courtlistener-cli tags list --limit 20
courtlistener-cli tags list --limit 100 --max-pages 2 --format csv --output ./output
courtlistener-cli tags list --format xlsx --output ./output
```

Get one tag:

```bash
courtlistener-cli tags get 123
```
